import { IRequest, IResponse } from "../../../interfaces";

export const index = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const store = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const show = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const unconfirmed = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const showUnconfirmed = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const types = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const fees = async (request: IRequest, h: IResponse) => {
    // @TODO
};
