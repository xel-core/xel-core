import { IRequest, IResponse } from "../../../interfaces";

export const status = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const syncing = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const configuration = async (request: IRequest, h: IResponse) => {
    // @TODO
};
