import { IRequest, IResponse } from "../../../interfaces";

export const index = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const show = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const blocks = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const voters = async (request: IRequest, h: IResponse) => {
    // @TODO
};

export const voterBalances = async (request: IRequest, h: IResponse) => {
    // @TODO
};
